class Solution {
public:
    string addBinary(string a, string b) {
        int ans = 0;
        int i = a.length();
        int j = b.length();
        int carry = 0;
            while(i > 0) || j > 0 || carry > 0) {
                if(i + j > 1){
                    ++carry;
                }

            }

        }
        
    }
};